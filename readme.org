Inspired by [[https://github.com/robxu9/bash-static][bash-static]] from robxu9.

I mostly use this with my RPi Zero W.

Uses static cross-musl-based toolchains provided by [[https://more.musl.cc/11/x86_64-linux-musl/armv6-linux-musleabihf-cross.tgz][armv6-linux-musleabihf-cross]] from musl.cc.

But it can be used elsewhere by utilizing the same toolchain downloaded from [[http://musl.cc/][musl.cc]].

Different toolchain (targetting other platform) may also work.

Features:
    + static bash with readline support.
    + static vim with normal features (can be built with huge also).
    + static rsync with xxhash support.
      * zstd, lz4, openssl are not supported yet
    + static coreutils

Only vim and bash has been somewhat tested.

Issues:
    + 06_xxhash +does not work+ works but xxhsum binaries are not static.
    + 07_rsync  builds +without+ with xxhash statically linked.

May fix this in future.

Or if you manage to build xxhsum statically, please let me know.
